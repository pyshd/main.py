1. Clone project `git clone git@github.com:pyshd/main.py.git`
2. Create venv `python -m venv venv`
3. Activate venv
4. Install requirements `pip install -r requirements.txt`
5. Run bot `python main.py`
